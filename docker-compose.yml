services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: emba_dashboard
      POSTGRES_USER: emba
      POSTGRES_PASSWORD: emba
    ports:
      - "5432:5432"
    volumes:
      - emba-postgres:/var/lib/postgresql/data

volumes:
  emba-postgres:
